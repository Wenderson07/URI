#include <stdio.h>
int main(){
	int code,number;
	float value,price,price_1,price_2,final_price;
	scanf("%d %d %f",&code, &number, &value);
	price_1 =number * value ;
	scanf("%d %d %f",&code, &number, &value);
	price_2 = number * value;
	final_price = price_1 + price_2;
	printf("VALOR A PAGAR: R$ %.2f\n",final_price);
	return 0;

}
