#include <stdio.h>
 
int main() { 
     char name;
     
  double salary,sales,x;
 
  scanf("%s %lf %lf", &name, &salary, &sales);
 
  x = salary + (15*sales)/100;
 
   printf("TOTAL = R$ %.2f\n",x);
 
    return 0;
}
